# ~integers~, ~floats~, ~booleans~, ~strings~

int() # infinite negative(-) to infinite positive(+) numbers
      # ex: -30009, 482, 0

float() # any number that contains a decimal point
        # ex:-100.01, 342.7, 1.000001
        
bool() # only two values: "True" or "False"
       # ex: 3>4 = False, 10!=30 = True, 5<4 = False
     
str() # strings are a sequence of characters, using either double("") or single('') quotes
      # ex: "Hello", "Goodbye", "How are you doing, my dear?"
      
